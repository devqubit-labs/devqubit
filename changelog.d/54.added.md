Added `devqubit-cudaq` adapter for NVIDIA CUDA-Q. Track `sample()` and `observe()` executions with automatic kernel artifact capture (JSON, MLIR, QIR), target snapshots, and UEC-compliant result envelopes. Install with `pip install "devqubit[cudaq]"`.
