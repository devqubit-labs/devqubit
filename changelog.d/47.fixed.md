Fixed run lifecycle: ``RUNNING`` state is now persisted immediately on context entry, SQLite connection leaks in multi-threaded usage are resolved (including a Python 3.12+ ``WeakSet`` crash), and all exceptions inherit from ``DevQubitError``.
